import React from 'react'

const Coverer = () => {
  const styles = {
    width: '100vw',
    height: '100vh',
    background: 'rgba(0, 0, 0, 30%)',
    position: 'absolute',
    top: '0px',
    right: '0px',
    zIndex: '70',
  }
  return <div style={styles}></div>
}

export default Coverer
